# Endrig's bot documents (verbatim copies)

Received 25 Sep 2026 from Endrig, who maintains the Discord bot (`Endrig7/clipcore`). Kept here unedited
because they are the contract our tests are written against. If Endrig sends a newer version, replace the
file and let the tests tell you what changed: `test/helpers/bot-contract.mjs` encodes the rules in these
files, and `test/helpers/fake-bot.mjs` behaves the way they describe.

Our plan for using them: [`../bot-integration-plan.md`](../bot-integration-plan.md).

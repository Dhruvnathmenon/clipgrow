{
  "name": "clipgrow",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema": "node scripts/bootstrap-db.mjs",
    "db:schema:remote": "node scripts/bootstrap-db.mjs --remote",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
